<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_default_hello') }}">Symfony</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Default
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_default_apple', {'number': 100}) }}">Apple</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_default_html') }}">HTML</a></li>
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Random
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_random_number', {'max': 1000}) }}">Number</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_random_million') }}">Million</a></li>
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Posts
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_post_list') }}">List</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_post_create') }}">Create</a></li>
                        </ul>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_user_index') }}">Users</a></li>
                    {% endif %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_user_edit', {'id': app.user.id}) }}">Profil</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_registration_logout') }}">Logout</a></li>
                {% else %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_registration_login') }}">Login</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Register</a></li>
                {% endif %}
                {% if app.request.session.get('_locale') == 'en' %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_locale_set', {'locale': 'fr'}) }}?route={{ app.request.attributes.get('_route') }}"><span class="fi fi-fr"></span></a></li>
                {% else %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_locale_set', {'locale': 'en'}) }}?route={{ app.request.attributes.get('_route') }}"><span class="fi fi-gb"></span></a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>